import { UmlsTerminiologyProvider } from './umls-terminiology-provider';

describe('UmlsTerminiologyProvider', () => {
  it('should create an instance', () => {
    expect(new UmlsTerminiologyProvider()).toBeTruthy();
  });
});
