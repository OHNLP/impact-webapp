import { MiddlewareRestProvider } from './middleware-rest-provider';

describe('MiddlewareRestProvider', () => {
  it('should create an instance', () => {
    expect(new MiddlewareRestProvider()).toBeTruthy();
  });
});
